// import PropTypes from 'prop-types';
import {  } from './Blog.styled';

const Blog = () => {
  return (
    <>Blog</>
  );
};

// Blog.propTypes = {
//   type: PropTypes.string,
//   width: PropTypes.number,
//   onClick: PropTypes.func,
//   children: PropTypes.node,
// };

export default Blog;