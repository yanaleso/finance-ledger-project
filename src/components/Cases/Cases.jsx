// import PropTypes from 'prop-types';
import {  } from './Cases.styled';

const Cases = () => {
  return (
    <>Cases</>
  );
};

// Cases.propTypes = {
//   type: PropTypes.string,
//   width: PropTypes.number,
//   onClick: PropTypes.func,
//   children: PropTypes.node,
// };

export default Cases;